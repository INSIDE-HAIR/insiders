// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// Configuración para usar múltiples archivos schema
// Con prismaSchemaFolder habilitado, todos los archivos .prisma en este directorio
// se cargan automáticamente
